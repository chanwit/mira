REPO = "chanwit"
TAG  = "v1"

task(up) {
	docker machine env dragon
	println REPO
	docker service create (
		name:  nginx,
		image: nginx,
	)
}

task(down) {
	docker machine env dragon

	docker service rm nginx
}